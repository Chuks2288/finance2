
type Props = {
    id: string;
    category: string | any;
    categoryId: string | any;
}

export const CategoryColumn = ({
    id,
    category,
    categoryId
}: Props) => {
    return (
        <div>

        </div>
    )
}